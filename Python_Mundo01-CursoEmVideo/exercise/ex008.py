# Create a program that reads a distance in meters and displays convection in centimeters and millimeters:

dataInput = float(input('Enter the distance in meters: '))
print(f'Converting {dataInput} meters into centimeters is {dataInput * 100} and in melimitres it is {dataInput * 1000}')
