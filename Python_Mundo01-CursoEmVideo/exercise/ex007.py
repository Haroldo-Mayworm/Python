# Create a program that reads two grades from a student and displays the average of the grades

gradeInput01 = float(input('Enter a grade: '))
gradeInput02 = float(input('Enter another grade: '))
print(f'The average grade is: {(gradeInput01 + gradeInput02) / 2}')
