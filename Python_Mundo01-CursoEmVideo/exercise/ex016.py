# Create a program that reads a real number and displays the entire portion of the number

import math

numberInput = float(input('Enter a number: '))

numberInt = math.floor(numberInput)

print(numberInt)
